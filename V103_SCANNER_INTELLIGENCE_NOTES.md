# V103 Scanner Intelligence

- Selects the sharpest source from a short camera burst and a high-resolution still.
- Preserves flash-aware capture and professional perspective correction.
- Builds shadow-normalized, sharpened, adaptive black-and-white, and clean-color OCR sources.
- Reads camera scans page by page and keeps OCR layout evidence.
- Uses field-level consensus and validates trucking-specific values before import.
- Keeps PDF native text as the first source and does not alter Logbook data without review.
