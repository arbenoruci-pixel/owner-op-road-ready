import fs from 'node:fs';
import assert from 'node:assert/strict';
const screen=fs.readFileSync('source/src/modules/owneros/OwnerOperatorOSV102.jsx','utf8');
const component=fs.readFileSync('source/src/modules/owneros/DotAuditCenterV109714.jsx','utf8');
const css=fs.readFileSync('source/src/modules/owneros/dotAuditCenterV109714.css','utf8');
assert.ok(screen.includes("import DotAuditCenterV109714 from './DotAuditCenterV109714.jsx'"));
assert.ok(screen.includes("tab==='audit' && <DotAuditCenterV109714"));
for(const token of ['eventDate','logDate','dutyDate','dailyMilesByDay','Rate Confirmation','BOL','POD','Fuel','Inspection','Billing','Open full Logbook day'])assert.ok(component.includes(token),`missing ${token}`);
assert.ok(component.includes("docsByLoad.get(base.loadNo)"),'full load documents must follow the load into the selected event day');
assert.ok(component.includes("invoiceRows=invoices.filter"),'billing must follow the load into the selected event day');
assert.ok(component.includes("fuelRows=fuel.filter"),'fuel must follow the load into the selected event day');
for(const token of ['.dot-week-list-v109714','.dot-day-list-v109714','.dot-proof-grid-v109714','.dot-docs-v109714'])assert.ok(css.includes(token),`missing ${token}`);
console.log('PASS — DOT audit day contains Logbook, miles and the complete load packet on the actual event date');
