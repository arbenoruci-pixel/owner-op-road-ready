# v95.92 — Static DOT document HTML

- Replaced JavaScript popup dependency in the shared DOT HTML package.
- “Open document” now jumps to a self-contained document section inside the same HTML file.
- Saved images render directly in the package.
- Saved PDFs use static object/iframe embedding with an original-file fallback.
- Added Back to documents and Open / save original file actions.
- No duty events, HOS logic, signing, routes, loads, miles, or wallet data were changed.
